{
  "$schema": "./schema.json",
  "artifacts": [
    {
      "id": "dist",
      "name": "dist",
      "include": ["dist/*.mjs"],
      "compression": "gzip"
    },
    {
      "id": "npm-package",
      "name": "npm package",
      "include": ["bin/**", "dist/**", "schema.json", "README.md"],
      "compression": "gzip"
    }
  ]
}
