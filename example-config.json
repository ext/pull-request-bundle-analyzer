{
  "$schema": "./schema.json",
  "bundles": [
    {
      "id": "dist",
      "name": "dist",
      "include": ["dist/*.mjs"],
      "compression": "gzip"
    },
    {
      "id": "npm-package",
      "name": "npm package",
      "include": ["*.tgz"],
      "compression": false
    }
  ]
}
